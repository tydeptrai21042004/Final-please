# Experiment manifests

Generate a controlled manifest with `python -m tools.run_fair_suite`. The runner
writes JSON and CSV manifests, a compatibility report, and a protocol summary.
Use `python -m tools.verify_fairness` before interpreting benchmark results.
